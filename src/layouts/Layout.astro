---
import '../styles/global.css';
import { product } from '../config/product';

interface Props {
	title?: string;
	description?: string;
	/** 'support' omits price offers from JSON-LD — /support is the App Store
	    Support URL and must stay free of pricing (see drafts/support-page-th.md) */
	variant?: 'product' | 'support';
}

const {
	title = "บิลง่าย (BillNgai) 2.0.3 สำหรับ Mac — เอกสารสำหรับฟรีแลนซ์ไทย",
	description = "BillNgai Mac 2.0.3: ใบเสนอราคา ใบแจ้งหนี้ และใบเสร็จธรรมดาสำหรับผู้ไม่จด VAT รับเต็มจำนวนเป็นเงินบาท ไม่รองรับใบกำกับภาษี พักประมาณการภาษี ซิงก์คลาวด์ และ e-Tax อ่านข้อจำกัดก่อนอัปเดต",
	variant = 'product'
} = Astro.props;

const localOffer = product.prices.local;
const proOffer = product.prices.pro;

// Construct Schema.org JSON-LD for AI search engines (AIEO) & Google
const schemaMarkup = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "บิลง่าย (BillNgai)",
  "softwareVersion": variant === 'support' ? undefined : `macOS ${product.downloads.mac.version}; Windows ${product.downloads.windows.version}`,
  "operatingSystem": "macOS, Windows",
  "applicationCategory": "BusinessApplication, FinanceApplication",
  "description": description,
	"image": new URL("/assets/brand/characters/modern-charector.png", Astro.site || "https://billiong-landing.pages.dev").href,
	"url": new URL(Astro.url.pathname, Astro.site || "https://billiong-landing.pages.dev").href,
  "featureList": [
    "Mac Direct 2.0.3: ใบเสนอราคา ใบแจ้งหนี้ และข้อมูลรายรับในเครื่อง",
    "ใบเสร็จธรรมดา: ผู้ยืนยันว่าไม่จด VAT รับเต็มจำนวนเป็นเงินบาทเท่านั้น",
    "ผู้ใช้ตรวจภาษีหัก ณ ที่จ่ายก่อนออกใบเสร็จ ไม่ใช้อัตรา 3% อัตโนมัติ",
    "จัดประเภทเงินได้ได้ใน Local โดยไม่ต้องซื้อ Pro",
    "พักประมาณการภาษี ซิงก์และกู้คืนคลาวด์ และ e-Tax รวมถึง Pro",
    "Windows 2.0.1 Beta และ Mac App Store ยังไม่ได้อัปเดต 2.0.3"
  ],
  ...(variant === 'support' ? {} : {
  "offers": [
    {
      "@type": "Offer",
      "name": "BillNgai Local",
      "price": localOffer.earlyBird,
      "priceCurrency": "THB",
      "priceValidUntil": "2027-12-31",
      "availability": "https://schema.org/InStock"
    },
    {
      "@type": "Offer",
      "name": "BillNgai Pro Early Bird Reservation",
      "price": proOffer.earlyBird,
      "priceCurrency": "THB",
      "priceValidUntil": "2027-12-31",
      "availability": "https://schema.org/PreOrder"
    }
  ]
  })
};

const ogImageUrl = new URL("/assets/brand/characters/modern-charector.png", Astro.site || "https://billiong-landing.pages.dev").href;
const pageUrl = new URL(Astro.url.pathname, Astro.site || "https://billiong-landing.pages.dev").href;
---

<!doctype html>
<html lang="th">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/logo.svg" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- SEO Meta Tags -->
		<title>{title}</title>
		<link rel="canonical" href={pageUrl} />
		<meta name="description" content={description} />
		<meta name="keywords" content="ใบเสร็จธรรมดา, ใบแจ้งหนี้, ใบเสนอราคา, ฟรีแลนซ์, macOS, Local-First, พร้อมเพย์ QR, BillNgai" />
		<meta name="robots" content="index, follow" />
		
		<!-- Open Graph / Facebook -->
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:type" content="website" />
		<meta property="og:url" content={pageUrl} />
		<meta property="og:image" content={ogImageUrl} />
		
		<!-- Twitter -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={ogImageUrl} />
		
		<!-- AIEO (AI Engine Optimization) JSON-LD -->
		<script type="application/ld+json" set:html={JSON.stringify(schemaMarkup)} />

		<!-- Brand fonts are self-hosted (see global.css @font-face) — no external font requests -->
	</head>
	<body class="font-sans antialiased bg-[#FFF9F3] text-[#2D3436] selection:bg-[#FF6B00]/20 selection:text-[#E05E00]">
		<slot />
	</body>
</html>
